# secrets — NEVER commit
.env
.env.*
**/.env
docs/.env

# deps & build
node_modules/
**/node_modules/
.vercel/

# cloned upstream repos (not ours)
.targets/

# local agent/ruflo state
.swarm/
.claude/
.claude-flow/
**/.claude-flow/

# os & misc
.DS_Store
*.log
*.zip
ruvector.db

# local agent DB + model cache + per-run build outputs (all regenerable, not source)
agentdb.rvf
agentdb.rvf.lock
kb/models-cache/
explainer-builds/

# heavy regenerable studio media (NotebookLM-style audio/video overviews) — keep images, not these
*.m4a
*.mp4
*.mov
*.wav
*.webm
.netlify/

# npm pack tarballs
*.tgz
